CREATE TABLE address (
                         id SERIAL PRIMARY KEY,
                         zip_code VARCHAR(255),
                         city VARCHAR(255),
                         street VARCHAR(255),
                         house_number VARCHAR(255)
)